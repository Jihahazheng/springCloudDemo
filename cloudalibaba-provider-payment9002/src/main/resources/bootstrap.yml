server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #        server-addr: localhost:8848 #配置Nacos地址
        server-addr: 192.168.255.152:1111 #配置nginx地址，做集群

management:
  endpoints:
    web:
      exposure:
        include: '*'




